# fastlane/.env 로 복사해서 값 채우기 (git에 커밋하지 말 것!)
# ASC API 키 발급: App Store Connect → Users and Access → Integrations → App Store Connect API
# 권한은 App Manager 이상. .p8 파일은 한 번만 다운로드 가능.
ASC_KEY_ID=YOUR_KEY_ID
ASC_ISSUER_ID=YOUR_ISSUER_ID
ASC_KEY_PATH=fastlane/AuthKey.p8
